entrega_fecha:
  type:         text
  function:     validate_textdate
  max_length:   8

entrega_hora:
  type:         text
  function:     validate_texttime
  max_length:   4

entrega_am_pm:
  type:         select
  options:      hora_am_pm_options

observaciones:
  type:         text
  acts_as:      textarea
  mandatory:    false
  value:        ""

